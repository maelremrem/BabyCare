# Legacy BabyCare updaters run `npm rebuild --build-from-source` after extraction.
# Release archives already contain a verified native binary, so lifecycle scripts
# must stay disabled to keep those updates offline and immutable.
ignore-scripts=true
audit=false
fund=false
update-notifier=false
